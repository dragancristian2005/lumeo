.user-message {
  text-align: right;
  margin-left: auto;
  background: theme("colors.primary");
  color: white;
  border-radius: 1rem;
  padding: 0.5rem 1rem;
  max-width: fit-content;
}

.other-message {
  text-align: left;
  margin-right: auto;
  background: theme("colors.primary");
  
  color: white;
  border-radius: 1rem;
  padding: 0.5rem 1rem;
  max-width: fit-content;
}

