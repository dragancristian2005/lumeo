<main *ngIf="authService.user$ | async as user" class="flex flex-row h-screen">
  <nav>
    <app-navigation></app-navigation>
  </nav>
  <div class="h-screen">
    <router-outlet></router-outlet>
  </div>
</main>

<div
  *ngIf="
    (authService.user$ | async) === null ||
    (authService.user$ | async) === undefined
  "
>
  <router-outlet></router-outlet>
</div>
